TASK: Execute the next phase of the couples-scrapbook bug fix + feature plan. Read progress.json to determine which phase to work on.

FIRST (mandatory, every run):
1. Read /Users/bob/.openclaw/workspace/repos/couples-scrapbook/progress.json — tells you which phase to work on
2. Read /Users/bob/.openclaw/workspace/repos/personal/sessions/couples-scrapbook/plan.md — detailed instructions for current phase
3. Read /Users/bob/.openclaw/workspace/repos/couples-scrapbook/KNOWN-ISSUES.md — safety rules and regression risks
4. cd /Users/bob/.openclaw/workspace/repos/couples-scrapbook && git log --oneline -5 — see what prior runs shipped
5. cd /Users/bob/.openclaw/workspace/repos/couples-scrapbook && git branch — confirm you're on design-audit

CRITICAL SAFETY RULES:
- All work on design-audit branch. Do NOT touch main until Phase 6.
- Run pnpm build after EVERY set of changes. Never commit if build fails.
- Read KNOWN-ISSUES.md BEFORE editing any file — it lists regression risks.
- One commit per phase. Commit message format: "fix: [description] ([bug IDs])" or "feat: [description]"
- Do NOT run supabase db push — Elijah does that manually.
- Existing scrapbooks must still work. Never remove or rename the music_id column.

PHASE DISPATCH:
Read currentPhase from progress.json. Execute ONLY that phase's instructions from plan.md.

- If currentPhase == 1: Already done. Update progress.json to phase 2 and exit.
- If currentPhase == 2: Upload reliability (H1, H4, M3). Follow Phase 2 in plan.md exactly.
- If currentPhase == 3: Cleanup + responsive (M5, M1, H3). Follow Phase 3 in plan.md exactly.
- If currentPhase == 4: Custom music upload. Follow Phase 4 in plan.md exactly. This is the largest phase — read ALL 10 files listed before writing any code.
- If currentPhase == 5: Integration check + browser testing. Follow Phase 5 in plan.md. Start dev server, use browser tool to check pages at desktop (1280x800) and mobile (390x844) viewports.
- If currentPhase == 6: Merge to main. Follow Phase 6 in plan.md exactly.
- If currentPhase > 6: All done. Update progress.json, send final completion message, exit.

AFTER EACH PHASE (non-negotiable):
1. Run: pnpm build — must be 0 errors
2. Run: pnpm lint (or npx eslint src) — 0 errors required (warnings OK)
3. Commit the changes with a descriptive message
4. Update progress.json:
   - Move current phase status to "done" with completedAt timestamp and commit hash
   - Increment currentPhase by 1
   - Update metrics (fixesApplied, featuresApplied, buildClean)
   - Set lastRunResult with a summary of what happened
   - Set lastRunAt to current time
   - Clear blockers if resolved
5. Commit progress.json: git add progress.json && git commit -m "chore: update progress — phase N complete"

IF STUCK:
- If a specific item can't be completed, add it to blockers[] in progress.json
- Increment consecutiveBlockerRuns
- Move to the next item in the phase's remaining[] list
- If ALL items in a phase are blocked, do NOT increment currentPhase — leave it for the audit cron to handle
- If consecutiveBlockerRuns >= 3, send alert message before doing anything else

RULES:
- Do NOT skip the FIRST read steps. Every run must re-read progress.json and plan.md.
- Do NOT work on multiple phases in one run. One phase per run, then exit.
- Do NOT refactor or "improve" code beyond what the plan specifies. Fix the bugs, add the feature, nothing else.
- If plan.md says to read a file before editing it, read it.
- For Phase 4 (custom music): read ALL 10 target files before writing any code. This is a cross-cutting feature.

COMPLETION CHECK:
- State which phase was executed
- State commit hash
- State pnpm build result (0 errors or list errors)
- State what was completed vs what remains
- Confirm progress.json was updated

⚠️ MESSAGING (mandatory for every run):
Send status to BOTH:
1. message tool: action="send", channel="telegram", target="5776469042"
2. message tool: action="send", channel="bluebubbles", target="+18083483131"
Format: "🔄 Scrapbook loop — Phase [N] [done/partial/blocked]. [1-line summary]. Build: [clean/errors]. Commit: [hash]"
