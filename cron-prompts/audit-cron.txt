TASK: Audit the couples-scrapbook coding loop — verify recent changes, fix issues, check for regressions, alert if blocked.

FIRST (mandatory):
1. Read /Users/bob/.openclaw/workspace/repos/couples-scrapbook/progress.json — current state
2. Read /Users/bob/.openclaw/workspace/repos/couples-scrapbook/KNOWN-ISSUES.md — safety rules + testing checklist
3. cd /Users/bob/.openclaw/workspace/repos/couples-scrapbook && git log --oneline -10 — recent commits
4. cd /Users/bob/.openclaw/workspace/repos/couples-scrapbook && git branch — confirm design-audit

CRITICAL RULES:
- Do NOT advance phases. The loop cron does that.
- Do NOT duplicate work — verify and fix only.
- If currentPhase > maxPhase: run final validation, send completion summary, then disable both crons.
- All work on design-audit branch until currentPhase == 6 AND Phase 6 is done.

AUDIT CHECKLIST:

1. **Build clean?**
   cd /Users/bob/.openclaw/workspace/repos/couples-scrapbook && pnpm build
   If errors: fix them, commit with "fix(audit): [description]", re-run build.

2. **Lint clean?**
   pnpm lint (or npx eslint src)
   If errors: fix, commit.

3. **Verify completed phases (grep-based):**
   For each phase marked "done" in progress.json, verify:
   - Phase 1: grep "Petit_Formal_Script" src/app/layout.tsx && grep "loop = true" src/components/music-player/index.tsx
   - Phase 2: grep "async function DELETE" src/app/api/scrapbooks/route.ts && grep "Uint8Array" src/app/preview/page.tsx && test -f supabase/migrations/002_delete_policy.sql
   - Phase 3: confirm NO "orientations" useState in book-viewer && grep "usePortrait={isPortrait}" src/components/book-viewer/index.tsx
   - Phase 4: test -f supabase/migrations/003_custom_music.sql && grep "customMusicUrl\|custom_music_url" src/app/api/scrapbooks/route.ts
   - Phase 5: check if browser tests were run (look for screenshots or test notes in progress.json)
   - Phase 6: check if main branch has merge commit

   If any verification fails for a "done" phase: fix the issue, commit, update progress.json.

4. **Check for common mistakes:**
   - grep "Buffer.from" src/app/preview/page.tsx — should NOT exist after Phase 2
   - grep "FileReader" src/app/preview/page.tsx — should NOT exist after Phase 2
   - grep 'usePortrait={true}' src/components/book-viewer/index.tsx — should NOT exist after Phase 3
   - grep "orientations" src/components/book-viewer/index.tsx — should NOT exist after Phase 3

5. **Metrics tracking:**
   Count commits since the loop started (git log --oneline a630d6f..HEAD | wc -l).
   Check: is fixesApplied in progress.json accurate?

6. **Blocker check:**
   If blockers[] is non-empty in progress.json:
   - Can you resolve any of them? If yes, fix and clear.
   - If consecutiveBlockerRuns >= 3: ALERT immediately before doing anything else.

7. **Completion check:**
   If currentPhase > maxPhase (6):
   - Run final full validation (build + lint + all grep checks)
   - If clean: disable both crons using openclaw cron disable [id] (read cronIds from progress.json)
   - Send final completion summary

IF CLEAN: Report "Audit pass", exit quickly. Do not waste tokens re-reading files unnecessarily.

IF ISSUES FOUND: Fix, validate fix, commit with "fix(audit): ..." prefix. Update progress.json if metrics changed.

⚠️ MESSAGING (mandatory):
Send status to BOTH:
1. message tool: action="send", channel="telegram", target="5776469042"
2. message tool: action="send", channel="bluebubbles", target="+18083483131"

If clean: "✅ Scrapbook audit pass — Phase [N] verified. Build clean. [fixesApplied]/[fixesTotal] fixes, [featuresApplied]/[featuresTotal] features."
If fixed: "🔧 Scrapbook audit — fixed [N] issues: [list]. Build clean now."
If blocked: "⚠️ Scrapbook BLOCKED — [blocker description]. Needs Elijah's input."
If complete: "🎉 Scrapbook ALL DONE — [N] phases complete. All fixes + custom music. Build clean on main. Pending: supabase db push + Vercel deploy."
